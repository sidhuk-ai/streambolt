import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com",'via.placeholder.com'], // Add the external image domain here
  }
};

export default nextConfig;
